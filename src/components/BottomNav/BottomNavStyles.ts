import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: 100,
    width: '100%',
    position: 'absolute',
    bottom: 0,
    backgroundColor: 'black',
   
    alignItems: "center"
  },
  middleIcon: {
    marginTop: 15,

  }
});
